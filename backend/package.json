{
  "name": "daily-paper-agent-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute daily-paper --remote --file=schema.sql",
    "db:init-local": "wrangler d1 execute daily-paper --local --file=schema.sql",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.95.0"
  }
}
